name: Docker Build

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    uses: acvigue/.github/.github/workflows/docker_build.yml@main
    with:
      arm_runner: "ubuntu-arm-m"
      amd_runner: "ubuntu-latest-m"
